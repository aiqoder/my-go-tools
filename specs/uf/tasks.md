# UF 用户反馈服务 - 任务清单

## 任务列表

### 阶段一：基础框架搭建

#### T1. 创建 go.mod 模块定义
- **描述**: 创建 `uf/` 目录和 `go.mod` 文件
- **验收标准**:
  - [x] `go.mod` 文件存在
  - [x] 模块路径为 `github.com/aiqoder/go-tools/uf`
  - [x] Go 版本 ≥ 1.18
- **依赖**: 无
- **实现备注**: 已创建 go.mod，模块名为 github.com/aiqoder/go-tools/uf，Go 版本 1.18

#### T2. 创建配置管理模块 (config.go)
- **描述**: 定义配置结构和默认配置
- **验收标准**:
  - [x] Config 结构体包含 BaseURL、Timeout、HTTPClient 字段
  - [x] 默认 BaseURL 为 `https://uf.yigechengzi.com/`
  - [x] 默认 Timeout 为 30 秒
  - [x] 包含 go doc 注释
- **依赖**: T1
- **实现备注**: 使用选项模式 (ClientOption) 自定义配置

#### T3. 创建类型定义模块 (types.go)
- **描述**: 定义通用请求/响应类型
- **验收标准**:
  - [x] 定义 Response 通用响应结构
  - [x] 参考 ACTIVATION_CHECK_API.md 风格
  - [x] 包含必要的 JSON 标签
- **依赖**: T1
- **实现备注**: 参考 ACTIVATION_CHECK_API.md 中的响应格式

#### T4. 创建错误处理模块 (errors.go)
- **描述**: 定义统一错误类型
- **验收标准**:
  - [x] 定义 Error 结构体
  - [x] 实现 Error() 和 Unwrap() 方法
  - [x] 包含常见错误创建函数
- **依赖**: T3
- **实现备注**: 包含 6 种错误码常量及便捷创建函数

### 阶段二：核心功能实现

#### T5. 实现客户端核心 (client.go)
- **描述**: 实现 Client 结构和核心方法
- **验收标准**:
  - [x] Client 结构体定义
  - [x] NewClient 构造函数，支持选项模式
  - [x] ClientOption 选项函数
  - [x] GetBaseURL() 方法
  - [x] SetHTTPClient() 方法
  - [x] 内部 doRequest() 方法
  - [x] 包含完整的 go doc 注释
- **依赖**: T2, T3, T4
- **实现备注**: 提供 GET、POST、PUT、DELETE 等方法，支持上下文

#### T6. 导出公共方法
- **描述**: 确保所有公共 API 可被外部调用
- **验收标准**:
  - [ ] 所有必要方法已导出（首字母大写）
  - [ ] 公共 API 有完整注释
- **依赖**: T5

### 阶段三：测试与文档

#### T7. 单元测试
- **描述**: 编写核心功能的单元测试
- **验收标准**:
  - [x] 创建 `client_test.go` 测试文件
  - [x] 测试 NewClient 默认配置
  - [x] 测试 ClientOption 选项
  - [x] 测试错误处理
  - [x] 测试覆盖率 ≥ 60%
- **依赖**: T5
- **实现备注**: 15 个测试用例，全部通过

#### T8. 示例代码
- **描述**: 编写使用示例
- **验收标准**:
  - [x] 创建 `examples_test.go`
  - [x] 包含典型使用场景示例
  - [x] 示例代码可运行（即使返回错误也算通过）
- **依赖**: T5
- **实现备注**: 8 个示例用例，全部通过

#### T9. README 文档
- **描述**: 编写包文档
- **验收标准**:
  - [x] 创建 `README.md`
  - [x] 包含安装方式
  - [x] 包含快速开始示例
  - [x] 包含 API 说明
- **依赖**: T5
- **实现备注**: 包含完整的 API 参考和扩展性设计说明

### 阶段四：代码质量

#### T10. 代码格式化检查
- **描述**: 确保代码符合 Go 规范
- **验收标准**:
  - [x] `gofmt -d` 无输出
  - [x] `go vet` 无警告
  - [x] `golint` 无警告（或可忽略）
- **依赖**: 所有任务
- **实现备注**: gofmt 和 go vet 均通过

## 执行顺序

```
T1 ─┬─> T2 ─> T5 ─> T6 ─> T7 ─> T8 ─> T9 ─> T10
     ├─> T3 ─┘
     └─> T4 ─┘
```

## 注意事项

1. 按依赖顺序执行任务
2. 每完成一个任务，在对应的验收标准上打勾
3. 如有需要，可细分任务
4. 保持代码风格与项目一致（中文注释、go fmt）
